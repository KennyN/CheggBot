const { DiscordAPIError } = require("discord.js");
const Discord = require('discord.js');
const { info } = require('../config.json');

module.exports = {
    name: 'update',
    description: 'Puts changelog into channel',
    
};